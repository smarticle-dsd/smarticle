export type AboutPageProps = {
  /**
   * Id prefix for DOM elements inside AboutPage component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside AboutPage component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the AboutPage
   */
  className?: string;
};
