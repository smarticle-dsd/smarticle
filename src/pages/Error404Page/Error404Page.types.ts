export type Error404PageProps = {
  /**
   * Id prefix for DOM elements inside Error404Page component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside Error404Page component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the Error404Page
   */
  className?: string;
};
