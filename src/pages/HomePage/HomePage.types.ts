export type HomePageProps = {
  /**
   * Id prefix for DOM elements inside HomePage component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside HomePage component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the HomePage
   */
  className?: string;
};
