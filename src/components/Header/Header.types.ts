export type HeaderProps = {
  /**
   * Id prefix for DOM elements inside Header component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside Header component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the Header
   */
  className?: string;
};
