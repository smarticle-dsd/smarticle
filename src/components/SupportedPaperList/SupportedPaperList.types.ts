export type SupportedPaperListProps = {
  /**
   * Id prefix for DOM elements inside SupportedPaperList component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside SupportedPaperList component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the SupportedPaperList
   */
  className?: string;
};
