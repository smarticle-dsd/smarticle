export type SummaryProps = {
  /**
   * Id prefix for DOM elements inside Summary component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside Summary component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the Summary
   */
  className?: string;
};
