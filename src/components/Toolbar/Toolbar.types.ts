export type ToolbarProps = {
  /**
   * Id prefix for DOM elements inside Toolbar component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside Toolbar component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the Toolbar
   */
  className?: string;
};
