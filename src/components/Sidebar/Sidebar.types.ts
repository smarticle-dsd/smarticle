export type SidebarProps = {
  /**
   * Id prefix for DOM elements inside Sidebar component
   */
  domID?: string;
  /**
   * Test prefix for `data-testid` attribute that will be appended to DOM elements inside Sidebar component
   */
  dataTestId?: string;
  /**
   * className that is added to the root element of the Sidebar
   */
  className?: string;
};
